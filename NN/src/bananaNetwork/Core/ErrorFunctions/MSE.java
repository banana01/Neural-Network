//package bananaNetwork.Core.ErrorFunctions;

/*
public class MSE implements ErrorFunction 
{
	private double error;
	private double elementCount;
	
	public double getTotalError()
	{
		return error/elementCount;
	}
	
    public void reset()
    {
    	error=0d;
    	elementcount=0d;
    }


    public double[]  calculatePatternError(double[] predictedOutput, double[] targetOutput)
    {
    	double elementError=new double[targetOutput.length];
    	 for (int i = 0; i < predictedOutput.length; i++) {
             elementError[i] =  targetOutput[i] - predictedOutput[i];
             error += elementError[i] * elementError[i];
             elementCount++;
         }
    	 return elementError;
    }

}
*/
